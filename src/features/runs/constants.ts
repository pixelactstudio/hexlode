/** Records (Inspect, Compare) kept per node for the Studio. Older ones are dropped. */
export const MAX_RECORDS_PER_NODE = 500
/** Errors and warnings kept per source item. */
export const MAX_MESSAGES_PER_SOURCE = 5
